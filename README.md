## 目录结构
```text
.
├── config.toml       # 通用 mise 配置，定义开发工具、dotfiles 及引导仓库
├── config.arch.toml  # Arch Linux 配置，定义通过 pacman 安装的软件包
└── conf.d/
    ├── dae.toml      # dae 网络代理、DNS、路由及代理规则
    └── keyd.toml     # keyd 键盘映射配置
```
